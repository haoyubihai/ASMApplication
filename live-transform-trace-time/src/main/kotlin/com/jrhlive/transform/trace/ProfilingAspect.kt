package com.jrhlive.transform.trace

object ProfilingAspect {

//
//    val  methodId = ProfilingAspect.generateMethodId(new MethodTag(fullClassName, simpleClassName, methodName, desc, parameterTypeList, desc.substring(desc.lastIndexOf(')') + 1)));
//
//    fun generateMethodId(MethodTag tag) {
//        int methodId = index.getAndIncrement();
//        if (methodId > MAX_NUM) return -1;
//        methodTagArr.set(methodId, tag);
//        return methodId;
//    }
}